# CL-NEXTSTEP

Cocoa binding for CommonLisp on macOS.  
It used CFFI.

## USAGE

1. build C shared library.
   ```
   $ cd /PATH/TO/cl-nextstep/
   $ cd C
   $ cmake -B build
   $ make -C build
   ```

